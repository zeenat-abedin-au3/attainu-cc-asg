"use strict";

var sum = function sum(num1, num2) {
  return num1 + num2;
};

var power = function power(num, exp) {
  return Math.pow(num, exp);
};

console.log(sum(2, 4));
console.log(power(2, 3));
